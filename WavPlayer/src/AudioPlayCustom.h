#ifndef AudioPlayCustom_H
#define AudioPlayCustom_H

/*
class AudioPlayCustom : public AudioPlaySdWav
{
public:
    bool seek(uint32_t pos){
	    if (!wavfile.available()) return false;
        return wavfile.seek(pos);
    }
    
};

*/
#endif